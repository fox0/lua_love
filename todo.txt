* экран загрузки (спрайты грузятся около 2 сек).
* нормировать угол поворота для функции стабилизации вращения
* переключение спрайтов бега. Добавить дельту для пороговых значений


* библиотека проверки коллизий
* библиотка теней - поиграться с свещением
//как вариант перед персонажем точечный источник свтеаы

* на первом экране меню с настройками (+сохранение в файловой системе)

* внизу сделать типа консоль
  -- print messages
    for i = 1,#text do
        love.graphics.setColor(255,255,255, 255 - (i-1) * 6)
        love.graphics.print(text[#text - (i-1)], 10, i * 15)
    end

    -- shapes can be drawn to the screen
    love.graphics.setColor(255,255,255)
    rect:draw('fill')
    mouse:draw('fill')

* soundtrack (the movie)
